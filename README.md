# Dmitry Pataiev test task

# Requirements:
  - docker
  - docker-compose
  - npm

# Run application

  - Clone repository
  - npm install
  - docker-compose up

After, GraphQL Playground will be available at: `localhost:5000/graphql`